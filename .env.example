# AWS Configuration for Integration Tests
# Copy this file to .env and customize for your AWS account:
#   cp .env.example .env
#
# Then edit .env with your specific AWS settings.
# The .env file is gitignored to keep your credentials local.

# AWS Profile (optional)
# If not set, uses AWS_PROFILE environment variable or "default"
# For SSO profiles, run: aws sso login --profile <profile-name>
#AWS_PROFILE=default

# AWS Region (required)
# The region where your test bucket exists
AWS_REGION=us-east-1

# S3 Test Bucket (required)
# This bucket must already exist in your AWS account
# Required IAM permissions: s3:PutObject, s3:GetObject, s3:DeleteObject, s3:HeadObject
BURST_TEST_BUCKET=burst-integration-tests

# S3 Key Prefix (optional)
# Prefix for organizing test files within the bucket
# Default: test-artifacts/
#BURST_TEST_KEY_PREFIX=test-artifacts/

# Output Directory (optional)
# Local directory for downloaded test files
# Default: /tmp/burst-test-output
#BURST_TEST_OUTPUT_DIR=/tmp/burst-test-output

# Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Create an S3 bucket in your AWS account
# 3. Edit .env and set BURST_TEST_BUCKET and AWS_REGION
# 4. If using SSO: aws sso login --profile <your-profile>
# 5. Run tests: bash tests/integration/test_downloader_phase1.sh
